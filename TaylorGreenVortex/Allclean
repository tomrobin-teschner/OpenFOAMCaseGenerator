# !/bin/sh
cd "${0%/*}" || exit  # Run from this directory
# ------------------------------------------------------------------------------

rm -rf 0.[0-9]* [1-9]* log logs processor*
cd postProcessing/
find . -type f ! -name '*.py' -delete
find . -type d -delete
cd ../

# ------------------------------------------------------------------------------
